ivory_serializer:
    visitors:
        xml:
            version: "1.0"
            encoding: UTF-8
            format_output: true
            root: result
            entry: entry
            entry_attribute: key
